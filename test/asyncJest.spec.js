const answerPhone = require("../src/asyncJest");



describe("o retorno do telefonema", () => {
  test("atende", () => {
    assert.fail();
    
  });
  test("ocupado", () => {
    assert.fail();
    
  });
});
